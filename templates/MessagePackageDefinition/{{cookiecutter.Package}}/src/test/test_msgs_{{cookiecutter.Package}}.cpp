
#include "{{cookiecutter.Package}}Msg.hpp"

#include <gtest/gtest.h>
#include <stdio.h>
using namespace fast::rf::messages::{{cookiecutter.Package}};
TEST(NewMessage, DefaultZeroConstructor) {
  ASSERT_TRUE(false);
}